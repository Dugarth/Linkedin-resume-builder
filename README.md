# Linkedin-resume-builder
This Flask app uses OpenAI's GPT models to tailor resumes for specific job postings. It analyzes job descriptions, generates custom summaries based on user resumes, and provides application links.
